# Generated by scripts/homebrew-formula.sh from the v0.2.2 release. Do not edit
# by hand: the next release overwrites it, and a hand-edited sha256 that no
# longer matches its URL fails at install time on someone else's machine.
class Agenthub < Formula
  desc "Local gateway between AI clients and MCP servers: one config, one credential store, one governance pipeline"
  homepage "https://github.com/dinstein/homebrew-agenthub"
  version "0.2.2"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/dinstein/homebrew-agenthub/releases/download/v0.2.2/agenthub-0.2.2-3f22e7e-darwin-arm64.tar.gz"
      sha256 "aa1c0f3ba2f12476ba49a16262d31e47d47f0f527ad4c49c42031085d25e23b2"
    end
    on_intel do
      url "https://github.com/dinstein/homebrew-agenthub/releases/download/v0.2.2/agenthub-0.2.2-3f22e7e-darwin-amd64.tar.gz"
      sha256 "b5f4661874f11e4a7607434875e565ec0fb73a050b4a97ae5f3ef83bcc41b27e"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/dinstein/homebrew-agenthub/releases/download/v0.2.2/agenthub-0.2.2-3f22e7e-linux-arm64.tar.gz"
      sha256 "f8d78c07cdb927a604d7ee71686e30dc62dab79e2478395490fc12b4398e3c60"
    end
    on_intel do
      url "https://github.com/dinstein/homebrew-agenthub/releases/download/v0.2.2/agenthub-0.2.2-3f22e7e-linux-amd64.tar.gz"
      sha256 "c9ac998f4df0442fdddfbf797b570b5630e5ff79d2829c4387b9f4a6993ad24a"
    end
  end

  def install
    bin.install "agenthub"
  end

  def caveats
    <<~EOS
      Point an AI client at the gateway (this edits that client's own config,
      so look at it first):

        agenthub client detect
        agenthub client connect claude-code --dry-run
        agenthub client connect claude-code

      Then restart the client. Servers added later need no further client
      changes — that is the point of the gateway.
    EOS
  end

  test do
    out = shell_output("#{bin}/agenthub --version")
    assert_match version.to_s, out

    # A release build must NOT be a dev build. The channel is a link-time flag,
    # so the only way to be sure the shipped artifact resolves the real data
    # directory is to ask the binary. Getting this wrong is silent: the user
    # keeps their config in AgentHubDev and reports that everything vanished.
    refute_match "(dev)", out

    # Exit 4 is "daemon offline", the correct answer on a machine that has
    # never started one. shell_output asserts the status itself, so this
    # proves the binary runs and classifies rather than only printing a
    # version string.
    shell_output("#{bin}/agenthub daemon status", 4)
  end
end
